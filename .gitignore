.nextflow*
work/
data/
results/
.DS_Store
testing/
testing*
*.pyc
__pycache__/
*.egg-info/
build/
dist/
null/
.nf-test/
.nf-test.log

# FETCH_ATLAS writes fetch_manifest.json into its output_dir. In the
# --atlas-path override branch that symlinks through to the source bundle,
# which would otherwise pollute the committed fixture.
tests/fixtures/tiny/tiny_atlas_bundle/fetch_manifest.json
